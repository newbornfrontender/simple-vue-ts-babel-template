'use strict';

import './index.css';

console.log(`NODE_ENV=${process.env.NODE_ENV}`);
console.log(`HASH_DIGEST=${process.env.HASH_DIGEST}`);
console.log(`HASH_DIGEST_LENGTH=${process.env.HASH_DIGEST_LENGTH}`);
